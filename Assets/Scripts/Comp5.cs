using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Comp5 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Component Component5 = new Component(75, "Component5", 0.0f, 
            0.0f, 250, false); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
